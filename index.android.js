'use strict';

import ReactNative, { AppRegistry } from 'react-native';
const ReactNativeIotDoorbell = require('./ReactNativeIotDoorbell');

AppRegistry.registerComponent('ReactNativeIotDoorbell', () => ReactNativeIotDoorbell);